name: PR Gate

on:
  pull_request:
    branches:
      - main
      - feature/*
      - release/*

permissions:
  contents: read

jobs:
  pr-gate:
    runs-on: ubuntu-latest
    steps:
      - name: Always succeed
        run: echo "PR Gate passed."
